# RSA-implementation
The program generates a public and private key pair, encrypts and decrypts a given message, and can also sign and verify it. You can also customize the number of bits to optimize the program's runtime.  
